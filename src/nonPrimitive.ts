let vegetable: string[] = ['potato', 'greenBanana', 'onion'];

vegetable.push('12');

//* Tuple

const friends: [string, number] = ['Ashraf', 12,];

friends[0] = 'karim'  //? here not allow the number!!
